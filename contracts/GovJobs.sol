pragma solidity ^0.4.23;


contract GovJobs {
    uint storedData;

    function voteJob() public {
    }

    function activeJobs() public {
    }

    function agency() public {
    }

    function projects() public {
    }

    function setupDownloadLink() public {
    }

    function get() public view returns (uint) {
        return storedData;
    }
}
